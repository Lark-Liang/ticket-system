server.port=8080
spring.application.name=ticket-system

# MySQL??
spring.datasource.url=jdbc:mysql://localhost:3306/ticket_system?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&autoReconnect=true&failOverReadOnly=false&maxReconnects=10
spring.datasource.username=ticket_user
spring.datasource.password=Ticket123!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ?????
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.hbm2ddl.auto=none

# SQL??
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql
spring.sql.init.continue-on-error=true
spring.sql.init.encoding=UTF-8

# MyBatis
mybatis.type-aliases-package=com.example.ticketsystem.entity
mybatis.configuration.map-underscore-to-camel-case=true

mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.configuration.default-statement-timeout=30
mybatis.configuration.default-fetch-size=100

# ??
logging.level.com.example.ticketsystem=DEBUG
logging.level.com.example.ticketsystem.mapper=TRACE
logging.level.org.springframework.jdbc=DEBUG
logging.level.org.springframework.boot.autoconfigure.jdbc=DEBUG
logging.level.org.springframework.boot=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.file.name=logs/ticket-system.log

# ??????
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# JWT??
jwt.secret-key=ticket-system-jwt-secret-key-2025-must-be-32-chars-long
jwt.access-token-expiration=7200
jwt.refresh-token-expiration=604800
jwt.token-prefix=Bearer 
jwt.token-header=Authorization